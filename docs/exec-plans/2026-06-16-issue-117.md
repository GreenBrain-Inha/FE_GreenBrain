# Issue #117: [feat] 랜딩페이지 구현

> https://github.com/GreenBrain-Inha/FE_GreenBrain/issues/117
> Labels: frontend | Milestone: -
> 생성일: 2026-06-16

## 목표
서비스 진입점 랜딩페이지 구현. 디자인 프로토타입 기반의 정적 마케팅 페이지.
API 연동 없이 애니메이션 + 레이아웃 위주.

## 관련 파일
- `app/page.tsx` — redirect → Landing 페이지로 교체
- `components/TokenRing.tsx` — Hero 섹션 링 애니메이션 컴포넌트 (신규)
- `components/RouteGuard.tsx` — `/` PUBLIC_PATHS 추가
- `app/layout.tsx` — Outfit, Inter 폰트 추가
- `app/globals.css` — @theme에 --font-outfit, --font-inter 등록

## 작업 분해
- [x] 브랜치 생성 (117-feat-랜딩페이지-구현)
- [ ] RouteGuard에 `/` 공개 경로 추가
- [ ] layout.tsx에 Outfit/Inter 폰트 등록
- [ ] globals.css @theme에 폰트 변수 추가
- [ ] TokenRing 컴포넌트 생성
- [ ] app/page.tsx — Landing 페이지 구현
- [ ] 커밋 & PR 생성

## 완료 기준
- Nav: `/login`, `/signup` 라우팅 동작
- TokenRing 애니메이션 (색상 전환 포함)
- 3개 섹션 + CTA + Footer 구현
- 반응형 (lg: 2-column ↔ 모바일 1-column)
- `style={{}}` → Tailwind 전환 (동적 계산·SVG 제외)
- lucide-react 미사용

## 설계 결정
- `app/page.tsx`를 클라이언트 컴포넌트로 전환 (`'use client'`) — 애니메이션 때문
- Outfit: 제목/기본폰트, Inter: 본문 override
- `/` 방문 시: 비로그인 → 랜딩 표시, 로그인 → RouteGuard가 `/chat` 리다이렉트
- TokenRing SVG 속성은 inline style 유지 (SVG presentation attribute)
- 복잡한 radial-gradient, clamp() 폰트도 inline style 유지
