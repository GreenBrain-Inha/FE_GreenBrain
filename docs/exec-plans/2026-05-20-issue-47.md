# Issue #47: [feat] 채팅 전송 시 선택된 모델 API 연동

> https://github.com/GreenBrain-Inha/FE_GreenBrain/issues/47
> Labels: frontend | Milestone: -
> 생성일: 2026-05-20

## 목표
모델 선택 드롭다운(#46)에서 선택한 모델을 채팅 전송 API request body에 포함해 실제 LLM 응답에 반영

## 관련 파일
- `app/chat/page.tsx` — handleSubmit의 POST body에 `model_id: selectedModel` 추가 (173번 줄)

## 작업 분해
- [x] `handleSubmit` POST body에 `model_id: selectedModel` 추가

## 완료 기준
- 채팅 전송 시 request body에 `model_id` 필드 포함
- 미선택 시 `DEFAULT_MODEL`(`openai/gpt-5.5`) 기본값 전송 (기존 state 초기값 유지)
- 기존 로딩·에러 동작 유지
