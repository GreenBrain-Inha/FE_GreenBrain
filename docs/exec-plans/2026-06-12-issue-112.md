# Issue #112: [feat] 랜딩페이지 구현 — 루트(/) 경로 서비스 소개 화면

> https://github.com/GreenBrain-Inha/FE_GreenBrain/issues/112
> Labels: frontend | Milestone: -
> 생성일: 2026-06-12

## 목표
루트(/) 경로에서 로그인 페이지 대신 서비스 소개 랜딩페이지를 표시한다.
비로그인 사용자에게 Anoixi 서비스를 소개하고 로그인/회원가입으로 유도한다.

## 관련 파일
- `app/page.tsx` — 현재 /login 리다이렉트만 있음 → 랜딩페이지로 교체
- `components/RouteGuard.tsx` — PUBLIC_PATHS에 '/' 추가 필요

## 작업 분해
- [ ] `components/RouteGuard.tsx`: PUBLIC_PATHS에 `'/'` 추가
- [ ] `app/page.tsx`: 'use client' 랜딩페이지 구현
  - isLoadingUser 중: 로딩 스피너 표시
  - user 있음: router.replace('/chat')
  - user 없음: 서비스 소개 콘텐츠 + 로그인/회원가입 버튼

## 완료 기준
- 루트(/) 접속 시 랜딩페이지 표시
- 서비스 소개 콘텐츠(제목, 핵심 기능 3가지) 포함
- 로그인 / 회원가입 버튼으로 이동 가능
- 이미 로그인한 사용자는 /chat으로 자동 이동
- 로딩 상태 구현
- 에러 상태 불필요 (auth 체크 실패 시 랜딩 표시가 올바른 동작)
